import 'package:flutter/material.dart';

class AppColors {
  // Warna background layar
  static const Color primaryPurple =
      Color(0xFF1900A7);

  // Warna untuk gradient background dari atas tengah ke bawah tengah
  static const List<Color> gradientBackground = [
              Color(0xFF1900A7),
              Color(0xFF0D0060),
  ];

  // Warna untuk efek lingkaran gradient
  static const Color blurCircleColor =  Color(0xFF5B33FF);

  // Warna untuk background putih
  static const Color whiteBackground = Color(0xFFF0F0F8);

  // Warna untuk danger
  static const Color dangerColor = Color(0xFFE74C3C);

  // Warna untuk success
  static const Color successColor = Color(0xFF4CAF50);

  // Warna untuk warning
  static const Color warningColor = Color(0xFFF5A623);

  // Warna untuk sakit
  static const Color purpleColor = Color(0xFF9B59B6);

  // Warna untuk secondary
  static const Color secondaryColor = Colors.grey;
}